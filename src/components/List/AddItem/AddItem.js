import React from 'react';

import Aux from '../../../hoc/Auxiliary/Auxiliary';

const addItem = props => (
    <Aux>
        <input type="text" />
        <button>ADD</button>
    </Aux>
);

export default addItem;